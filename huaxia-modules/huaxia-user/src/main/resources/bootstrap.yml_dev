spring:
  application:
    name: huaxia-user
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        namespace: HuaxiaYidianTong
        server-addr: localhost:8848
        group: DEFAULT_GROUP
      config:
        namespace: HuaxiaYidianTong
        server-addr: localhost:8848
        group: DEFAULT_GROUP
        file-extension: yml
        shared-configs:
          - data-id: huaxia-user-dev.yml
            group: DEFAULT_GROUP
            refresh: true